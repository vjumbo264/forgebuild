# Prompt History
- 2026-09-11 (type-1 build): "An Android version of ForgeBuild itself — a companion app for managing my ForgeBuild dashboard from my phone instead of the browser. It should connect to the vjumbo264/forgebuild GitHub repo the same way the web Dashboard does, using a GitHub PAT I enter once and it stores securely on-device (not asked for again unless I disconnect).
Core screens: a home screen where I can type a new app description (and optionally a slug) and generate/copy the type-1 build prompt; an apps list pulled from the apps/ folder in the repo; an app detail screen showing that app's releases (tagged <slug>-vN) with APK/manifest/source download links, a delete-version action, the prompt history, and the type-2 extend prompt generator with its free-text instruction field; a version screen for any in-progress build showing its BUILD_STATE.json and the type-3 resume prompt; and a delete-entire-app action with confirmation.
It should use the ForgeBuild Engine's Material 3 theme (dynamic color, light/dark), proper adaptive icon, and the cache-first/background-refresh data pattern for the apps and releases lists so it doesn't reload from GitHub every time I open it. Lightweight — this is a management/utility tool, not a heavy app. No slug — let the build session choose one once it's read this description."
