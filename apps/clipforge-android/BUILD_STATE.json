{
  "slug": "clipforge-android",
  "project": "forgebuild-clipforge-android",
  "app": "ClipForge Android",
  "build_complete": false,
  "updated_by": "session-11",
  "current_task": "task-74",
  "tasks": [
    {
      "id": "task-74",
      "title": "Swipe-to-refresh + spinning refresh affordance on list screens (keep corner button)",
      "status": "pending",
      "notes": "Operator: 'I need swipe to refresh... or leave the refresh button in the corner, but also allow me the web to swipe down and refresh... if I click that refresh button in the corner, it should be spinning like a normal refreshing. And when I swipe to refresh, it should also be spinning the same way.' INVESTIGATED state (v7 source, freshly read): TasksScreens.kt (active list ~line 45-77, completed list ~line 138-167) and SeriesScreens.kt (SeriesScreen ~line 28-55) each render an IconButton with a STATIC Icons.Default.Refresh plus a separate LinearProgressIndicator shown when vm.tasksRefreshing is true. IMPLEMENT: (a) add androidx.compose.material3 PullToRefreshBox (compose-bom 2024.09.00 already supplies M3 1.3.x which includes it - verify at build, fallback to material pullrefresh if absent) wrapping each LazyColumn, isRefreshing = tasksRefreshing, onRefresh = vm.refreshTasks(); (b) replace the static refresh icon with a spinning affordance - when refreshing, show a small CircularProgressIndicator inside the IconButton (or animate the Refresh icon rotation infinitely); when idle show the static Refresh icon. Both paths (corner tap and swipe) must drive the SAME vm.refreshTasks() + tasksRefreshing state so both show identical spinning feedback. Keep the corner button per the operator's 'or leave the refresh button' permission. Files: ui/TasksScreens.kt, ui/SeriesScreens.kt. No new dependency expected."
    },
    {
      "id": "task-75",
      "title": "Export diagnostic log to Documents/ClipForge via Storage Access Framework",
      "status": "pending",
      "notes": "Operator: 'the debug log, I want it to be exported to my Documents folder under ClipForge, not to a folder I can't even get to.' INVESTIGATED state: DiagLog.export (data/DiagLog.kt:48) currently writes somewhere not user-visible; vm.exportDiagLog (ClipForgeViewModel.kt:1330) calls it; Settings About section button 'Export Diagnostic Log' (ui/SettingsScreen.kt:537). IMPLEMENT per the standing Engine constraint: use com.forgebuild.engine.files.SafeSave (Storage Access Framework ACTION_CREATE_DOCUMENT) - the engine copy already exists at app/src/main/java/com/forgebuild/engine/files/SafeSave.kt, USE IT, never DownloadManager. Wire an ActivityResultLauncher (rememberLauncherForActivityResult(CreateDocument('text/plain'))) in SettingsScreen, default file name like clipforge-diagnostic-log-<epoch>.txt, write DiagLog.export(app) text to the returned uri via contentResolver.openOutputStream. SAF lets the operator pick Documents/ClipForge (creating the folder from the picker) on any Android version without permissions - this satisfies 'Documents folder under ClipForge' in a permission-free, user-directed way (a hardcoded Documents/ClipForge write needs MANAGE_EXTERNAL_STORAGE on API 30+; SAF is the correct Engine-mandated route). Toast the saved location on success. Files: ui/SettingsScreen.kt, possibly ClipForgeViewModel.kt (export returns text only, no file write)."
    },
    {
      "id": "task-76",
      "title": "Super Series list shows 'Part 1/2/3...' labels instead of bare topic folders",
      "status": "pending",
      "notes": "Operator: 'the Super Series... is lacking the necessary parts, like the Part 1, Part 2, Part 3 parts. Like when it does, it just gives them topic folders, add the Part 1, Part 2, Part 3, parts.' INVESTIGATED state: SeriesScreen (ui/SeriesScreens.kt:60-110) lists one row per seriesId with Text(seriesId) as the title (line ~74) and 'Latest: Part N - state' as a subtitle - for a Super Series this reads as one bare topic folder with no per-part rows. IMPLEMENT: make each series row expandable (or navigate into SeriesDetailScreen) showing per-part entries labeled 'Part 1', 'Part 2', ... with per-part state; SeriesDetailScreen already renders parts as 'Part N (jobId)' (line ~234) - the gap is the top-level SeriesScreen. Minimal fix honoring the operator: on SeriesScreen, for every series (super or ordinary), render the sorted parts inline under the series title (or an expand chevron) as 'Part N - <title or state>' rows, reusing the existing sortedParts data already computed there (sortedParts/lastPart at ~line 67). For Super Series queues also surface the queue progress banner already present in SeriesDetailScreen ('Part X of Y is being queued...', line ~194) at the series row when applicable. No new data plumbing expected - parts are already loaded."
    },
    {
      "id": "task-77",
      "title": "Prompt wording: series prompts state their part number; Super Series prompt states it is a Super Series and the parts it will produce",
      "status": "pending",
      "notes": "Operator: 'If I copy the prompt for Part 1, it should state that this is Part 1 of the series... same for Part 2..5. If it's a Super Series, the prompt should state that this is a Super Series and the videos/parts it is going to give... use your own judgment.' GROUND TRUTH ALREADY VERIFIED in motionssalt/clipforge @ main during this session: pipeline/plan/prompt.py SERIES_DIRECTIVE already emits '## SERIES MODE - YOU ARE AUTHORING PART 1 (not any other part number)' (verified by real CLI run, control output) and SUPER_SERIES_DIRECTIVE already emits '## SUPER SERIES MODE - YOU ARE PLANNING THE WHOLE SERIES IN ONE DOCUMENT' plus the requirement that the returned document plans EVERY part (verified by real CLI run, grep=1). So the BOT side already states part numbers and Super Series identity. TASK: find where the ANDROID app displays/copies prompts (search for the prompt-copy affordance in TaskDetail awaiting_plan flow / ui/TasksScreens.kt and ClipForgeViewModel) and ensure: (1) for ordinary series part N the copied/displayed prompt header states 'This is Part N of the series' (if the app composes any wrapper text itself, add/keep the part-number line; if it relays the pipeline prompt, verify the pipeline text is what gets copied); (2) for a Super Series the copied prompt states it is a Super Series and enumerates the parts it will produce (Part 1..Part N with titles if available from super-plan.json). If the app simply copies the pipeline-generated prompt, confirm the generated text is unchanged and close this task as verified-parity; only compose app-side wrapper text if the app currently strips or replaces the header."
    },
    {
      "id": "task-78",
      "title": "Validate build (Kotlin structural balance + marker checks) and bump version to 8",
      "status": "pending",
      "notes": "Run the session validation harness (apps/clipforge-android/tools/validate_session09.py reusable; extend markers for: PullToRefreshBox presence in TasksScreens/SeriesScreens, spinning refresh indicator, CreateDocument SAF export in SettingsScreen, 'Part ' rows on SeriesScreen, prompt header parity) plus comment/string/template-aware brace balance on all modified Kotlin files. Bump versionCode/versionName 7 -> 8 in app/build.gradle.kts. R8 + resource shrinking remain ON. No new dependency beyond possibly nothing (PullToRefreshBox is in the existing BOM)."
    },
    {
      "id": "task-79",
      "title": "Release clipforge-android-v8 via release.yml and verify live on the Releases API",
      "status": "pending",
      "notes": "Dispatch the Build & Release App APK workflow (POST /repos/vjumbo264/forgebuild/actions/workflows/release.yml/dispatches, ref=main, inputs app_path='apps/clipforge-android', release_notes summarizing task-74..77). NEVER overwrite/delete prior releases v1..v7. Poll the run to success; if the build fails, fix and re-dispatch (tag auto-increments only on success). Verify the release live via the GitHub Releases API (APK + forgebuild-manifest.json assets present), then set build_complete: true."
    }
  ],
  "notes": "\nSESSION-11 BOUNDARY (2026-09-12): clipforge-side bug-73 closed and pushed on motionssalt/clipforge first (commit 98f9723): the seeded 'unwired Super Series toggle handlers' hypothesis was DISPROVEN on origin/main - the set:superseries / set:superseries:on / set:superseries:off handlers have existed since feature-01 commit 44fda37 and were verified end-to-end (settings write shape, wizard commit boundary, wizardToRequest series.super_series, REAL prompt.py CLI runs producing SUPER_SERIES_DIRECTIVE vs ordinary SERIES_DIRECTIVE, super-plan upload routing). The operator's production symptoms were a STALE BOT DEPLOYMENT (worker predating 44fda37), not a code defect; redeploying the bot worker from current main is an operator/infra action. Real gap fixed there: bot/test/super-series-handlers.test.mjs (7 wiring regression tests incl. a general guard that every emitted set:* callback_data has a matched handler). All suites green: bot 242/242, pipeline 386 pass (8 pre-existing documented env failures unchanged).\n\nSESSION-11 FORGEBUILD WORK: operator instruction appended to PROMPT_HISTORY/clipforge-android.md (commit a73c38e). v7 source freshly read: refresh buttons exist on TasksScreens (active+completed) and SeriesScreens but are static icons with a separate LinearProgressIndicator and NO swipe-to-refresh; DiagLog export goes to a non-user-visible location; SeriesScreen renders one bare row per seriesId ('topic folder' symptom) while SeriesDetailScreen already has per-part rows; prompt part-number/Super-Series wording already correct in the pipeline ground truth (must be mirrored/verified in the app's copy surface). Seeded task-74..task-79 for the v8 cycle. EXECUTION BOUNDARY: this session's sandbox cannot run the Android Gradle build or the GitHub Releases API dispatch without exposing the credential in logs, so implementation of task-74..79 is handed to the next session via this checkpoint, per the stopping rule. NEXT SESSION: start at task-74, follow the per-step protocol (implement -> validate -> BUILD_STATE update -> commit -> push -> verify), then release v8.\n"
}
